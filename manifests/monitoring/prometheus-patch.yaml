apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: k8s
spec:
  replicas: 1
  retention: 120h
  retentionSize: 12GiB
  storage:
    volumeClaimTemplate:
      spec:
        resources:
          requests:
            storage: 16Gi
