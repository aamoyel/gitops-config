apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gitops-config
  namespace: flux-system
spec:
  interval: 1m
  ref:
    branch: main
  url: https://github.com/aamoyel/gitops-config.git
  ignore: |
    /*
    !/bootstrap/fluxcd
    !/apps
    !/manifests
